query listSlider($input: ListSliderInput!) {
  listSlider(input: $input) {
    sliders {
      _id
      mediaId {
        url
      }
      position
      type
      redirectUrl
    }
    pagination {
      totalPage
      currentPage
      pageSize
    }
    totalItem
  }
}
