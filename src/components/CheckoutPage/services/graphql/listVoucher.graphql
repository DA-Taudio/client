query listVoucher($input: ListVoucherInput) {
  listVoucher(input: $input) {
    vouchers {
      _id
      code
      percent
      maxDiscount
      quantity
      productIds
      startTime
      endTime
      countHistory
    }
  }
}
